# Coral Brasil - dependencias do backend
# Versoes fixadas conforme o ambiente validado (Python 3.13).
# Django 5.1+ nao suporta Python 3.14 - use py -3.13 para criar o venv.
#
#   py -3.13 -m venv venv
#   .\venv\Scripts\activate
#   pip install -r requirements.txt
#   python backend\manage.py runserver
#
# Frontend: cd frontend && npm install && npm start

# --- Core web ---
Django==5.2.8
djangorestframework==3.17.1
django-cors-headers==4.4.0
asgiref==3.11.1
sqlparse==0.5.1
tzdata==2026.1

# --- Configuracao por ambiente (.env) ---
django-environ==0.12.0

# --- Banco de dados ---
# PostgreSQL e a fonte unica da verdade desde 25/07/2026 (docs/arquitetura.md).
# Configure DATABASE_URL no .env; sem ela o Django cai em SQLite, util so para
# rodar os testes rapido.
psycopg[binary]==3.2.9
# Neo4j e projecao derivada, nunca escrita direta. Driver bruto: nao ha backend
# de ORM do Django para Neo4j, o Cypher e escrito a mao em backend/db/.
neo4j==5.20.0

# --- Processamento de dados ---
numpy==2.4.4
pandas==2.3.3
xarray==2026.4.0
netCDF4==1.7.4
h5netcdf==1.8.1

# --- Ingestao de fontes externas ---
erddapy==3.1.1          # cliente ERDDAP (NOAA Coral Reef Watch)
copernicusmarine==2.3.0 # cliente oficial Copernicus Marine Service

# Raizes de CA. Ja vinha como dependencia transitiva, mas agora e usada
# diretamente: no Windows o urllib (por onde o pandas busca o CSV do ERDDAP)
# nao monta a cadeia sozinho. Ver backend/ingestao/certificados.py.
#
# Unica dependencia sem versao exata, de proposito: e uma lista de autoridades
# certificadoras, e congela-la significa envelhecer a confianca do projeto.
# Uma raiz nova em 2027 quebraria a ingestao com "unable to get local issuer
# certificate" ate alguem lembrar de subir o pin.
certifi>=2026.2.25

# --- Machine learning ---
scikit-learn==1.8.0
joblib==1.5.3

# --- Midia e relatorios ---
pillow==12.2.0
matplotlib==3.10.8

# ---------------------------------------------------------------------------
# Neo4j - configuracao e comandos uteis
# ---------------------------------------------------------------------------
# As credenciais agora saem de backend/.env (ver .env.example), nao mais de
# variaveis de sessao do PowerShell.
#
#   python backend/manage.py check
#   Test-NetConnection 127.0.0.1 -Port 7687   # confere se o Neo4j responde
#   python backend/manage.py neo4j_init
#   python backend/manage.py neo4j_seed
